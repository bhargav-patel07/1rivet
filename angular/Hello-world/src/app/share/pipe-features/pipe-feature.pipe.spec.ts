import { PipeFeaturePipe } from './pipe-feature.pipe';

describe('PipeFeaturePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFeaturePipe();
    expect(pipe).toBeTruthy();
  });
});
